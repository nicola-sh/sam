# Скопируйте в sam_app_data/config.yaml (не коммитьте пароли)

sam:
  last_export_dir: ""

vault:
  path: vault.enc

ssh:
  host: "10.11.44.10"
  port: 22
  username: "shirnin_nv"
  password: ""
  password_secret: ssh_password
  key_filename: ""

microservices:
  - id: atm-ddc
    name: ATM DDC Service
    service_dir: /srv_mproc/mproc/services/atm-ddc-service
    arch_subdir: /log_arch
    main_subdir: /log
    outputs:
      - id: DDC
        arch_prefix: atm-ddc
        main_name: atm-ddc
        main_only_today: true
      - id: DDC5556
        arch_prefix: atm-ddc5556
        main_name: atm-ddc5556
        main_only_today: true

  # Пример другого микросервиса:
  # - id: payment-api
  #   name: Payment API
  #   service_dir: /var/log/payment-api
  #   arch_subdir: /archive
  #   main_subdir: /current
  #   outputs:
  #     - id: main
  #       arch_prefix: payment
  #       main_name: payment.log
  #       main_only_today: true

upload:
  enabled: false
  host: "10.11.44.10"
  port: 22
  username: "shirnin_nv"
  password_secret: upload_password
  remote_dir: "/home/BELINVESTBANK/shirnin_nv"
