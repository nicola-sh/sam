# RegCon (модуль SAM)

Обезличивание локальных логов: PAN (Luhn), IP, пароли, подтверждение перед маской, CSV→Excel.

Используется как вкладка **«Обезличивание»** в `python -m sam`. Отдельное окно (отладка): `python -m regcon`.

Данные: `regcon_app_data/`. Не использует SSH и vault SAM.
