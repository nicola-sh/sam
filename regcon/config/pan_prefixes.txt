# Начальные цифры допустимых PAN (BIN / префиксы).
# Одна запись на строку; допускаются пробелы и комментарии (#).
# При сканировании: строка без любого префикса пропускается;
# кандидат проверяется Luhn только если номер начинается с префикса из списка.
#
# Пример (замените на полный справочник):
9112
911239
91123900
